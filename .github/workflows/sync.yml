name: Sync Fork

on:
  schedule:
    - cron: '0 10 * * *'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - uses: TG908/fork-sync@v1.1
        with:
          github_token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          owner: jaedb
          base: master
          head: master
